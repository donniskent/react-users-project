import AddUserForm from "./AddUserForm"

const AddUser = () => {
    //Probably wont need this since we arent trying to hide the add user form 
    // can put all the handlers and state stuff within the form

    return (

        <AddUserForm/>
    )

}
export default AddUser;